[project]
name = "vvp"
version = "0.1.0"
description = "VVP Monorepo - Verifiable Verified Party"
requires-python = ">=3.12"

# No direct dependencies - this is a workspace root
# Services have their own pyproject.toml files

[tool.setuptools]
py-modules = []
